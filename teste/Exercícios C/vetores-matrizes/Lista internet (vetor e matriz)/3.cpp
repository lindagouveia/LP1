#include <stdio.h>
#include <math.h>

int main()
{ 
	int x, y, i, V[4];
	
		for(i=0; i<4; ++i){
			
			scanf("%d", &V[i]);
			
		}
		
			x = V[2];
			y = V[3];
			
		printf ("%d", x+y);
	
	
		return 0;
}

