#include <stdio.h>
	int main(){
		
		int a, b, c, d;
			
			scanf("%i %i %i %i", &a, &b, &c, &d);
			
		if(a%2==0 && b%2==0 && c%2==0 && d%2==0)
			
			printf("%i", a+b+c+d);
		
		else
			
			printf("n soma");
		

	return 0;

}
