#include <stdio.h>
    int main(){

        float h, homem, mulher;

            scanf("%f", &h);

        homem = (72.7*h)-58;
        mulher = (62.1*h)-44.7;

            printf("peso ideal homem: %f\n peso ideal mulher: %f", homem, mulher);


    return 0;


    }
