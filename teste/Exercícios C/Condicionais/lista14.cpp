#include <stdio.h>
	int main(){
		
		int a, b, c, d;
			
			scanf("%i %i %i %i", a, b, c, d);
		
		if(a==1 && b==2 && c==3 && d==4)
			
			printf("senha correta");
		
		else
			printf("senha incorreta");
	
	
	return 0;
	
}
