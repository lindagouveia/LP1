#include <stdio.h>
    int main(){

        int a, b, soma, sub, mult, div;

            scanf("%i %i", &a, &b);

        soma = a+b;
        sub = a-b;
        mult = a*b;
        div = a/b;

            printf("%i %i %i %i", soma, sub, mult, div);


    return 0;



    }
